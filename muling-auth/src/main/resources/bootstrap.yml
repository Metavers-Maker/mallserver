spring:
  application:
    name: muling-auth
  profiles:
    active: dev
#日志配置，支持写入外部文件，方便问题排查
logging:
  file:
    max-history: 10d
    max-size: 50MB
  level:
    root: INFO
    io.jaegertracing: WARN